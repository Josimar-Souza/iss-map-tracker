import styled from 'styled-components';
import { MarkerIcon } from '@sanity/icons';

export const OverlayImage = styled.img`
  width: 10%;
`;

export const UserMarker = styled(MarkerIcon)`
  color: red;
  font-size: 30px;
`;
